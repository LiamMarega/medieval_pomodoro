targets:
  $default:
    builders:
      injectable_generator|injectable_builder:
        enabled: true
        generate_for:
          include:
            - lib/**.dart
            - tests/**.dart
      injectable_generator|injectable_config_builder:
        enabled: true
        generate_for:
          include:
            - lib/common/ioc.dart
